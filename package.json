{
  "name": "awtw-short-url-service",
  "version": "1.0.0",
  "description": "ASUS short URL service",
  "main": "dist/index.js",
  "scripts": {
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "build": "tsc"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^1.0.0"
  }
}
